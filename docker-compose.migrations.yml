version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Touring_Identity
      - Touring_DB=Touring_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
